apply plugin: 'ear'

dependencies {
    deploy project(path: ':web', configuration: 'archives')
    deploy project(path: ':ejb', configuration: 'archives')
}

ear {
    archiveName "openrewrite-usecase.ear"
}
